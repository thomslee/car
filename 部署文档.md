# 汽车档案系统 · 部署与运维文档

> 上线目标：腾讯云服务器（与 news-system 同机），服务端口 **8090**。
> 技术栈：Vue3 + Vant4 前端（静态构建） / FastAPI 后端 / MySQL（复用服务器已有 MySQL 容器）。

---

## 0. 部署状态（2026-09-06）

✅ **已部署上线**：`http://82.156.177.145:8090`

- 后端：`car-backend` 容器（127.0.0.1:8091，复用 `news_news_net` 网络连 `news-mysql`），镜像 `car-backend:latest`
- 前端：`/var/www/car/releases/v1`，`/var/www/car/current` 软链已切换
- nginx：`/etc/nginx/conf.d/car.conf`，监听 8090
- 数据库：`car_system` 库（17 张表）已建，种子数据（沃尔沃保养基准 8 条、参考价 7 条、ai_providers 2 条）已写入
- DeepSeek：线上已启用并配置 API Key（可在「设置 → 大模型配置」随时更换）
- 构建镜像时使用国内 PyPI 镜像（清华源），避免服务器访问 PyPI 过慢

---

## 1. 端口规划（与既有系统错开）

| 端口 | 用途 | 说明 |
|---|---|---|
| 80 | 资金管理系统 | nginx 反代 fund-app:8000，**勿动** |
| 8080 | 新闻系统 | 静态 + /api→127.0.0.1:8001，**勿动** |
| 8001 | news-backend | 仅绑 127.0.0.1 |
| **8090** | **汽车档案系统** | nginx 监听：静态文件 + /api→127.0.0.1:8091 |
| **8091** | **car-backend 容器** | 仅绑 127.0.0.1，不对外 |

云服务器安全组需放行 **8090**（80/8080 若已放行不受影响）。

---

## 2. 本机开发环境

| 项 | 值 |
|---|---|
| 代码目录 | `E:\car`（backend + frontend） |
| 数据库 | 本机 MySQL，库 `car_system`（root/123456，已建库） |
| 后端启动 | 双击 `E:\car\backend\start_backend.bat`（端口 8002） |
| 前端启动 | 双击 `E:\car\frontend\start_frontend.bat`（端口 5173，代理 /api→8002） |
| 一键启动 | 双击 `E:\car\start_all.bat` |

浏览器打开 `http://127.0.0.1:5173`，首次使用先注册账号。

> 注意：bat 脚本请保持现有编码，勿转 UTF-8（与 news 系统同样的坑）。

---

## 3. 服务器部署（腾讯云）

### 3.1 前置

- 服务器已有 docker + docker compose（news 系统在用）。
- 复用服务器已有 MySQL 容器（`news-mysql`，root/123456）。在容器内建库：
  ```bash
  docker exec news-mysql mysql -uroot -p123456 -e "CREATE DATABASE IF NOT EXISTS car_system DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci"
  ```

### 3.2 上传代码

在 `E:\car` 下执行（或任意 SFTP 工具）：

```bash
# 本机执行：先构建前端
cd E:\car\frontend && npm run build

# 上传（示例 scp，也可用 FileZilla）：
scp -r E:\car\backend ubuntu@<服务器IP>:~/car/
scp -r E:\car\frontend\dist ubuntu@<服务器IP>:~/car/frontend/
```

### 3.3 后端容器（docker compose）

服务器 `~/car/backend/docker-compose.yml`（新建）：

```yaml
services:
  car-backend:
    image: python:3.12-slim
    container_name: car-backend
    working_dir: /app
    volumes:
      - ./app:/app/app
      - ./data:/app/data
    command: >
      sh -c "pip install -r requirements.txt --quiet
      && uvicorn app.main:app --host 0.0.0.0 --port 8091"
    ports:
      - "127.0.0.1:8091:8091"
    environment:
      CAR_DB_HOST: news-mysql
      CAR_DB_PORT: "3306"
      CAR_DB_USER: root
      CAR_DB_PASSWORD: "123456"
      CAR_DB_NAME: car_system
      CAR_JWT_SECRET: "请改为随机长字符串"
    restart: unless-stopped
    networks:
      - car-net

networks:
  car-net:
    external: true
    name: news_news_net   # ✅ 2026-09-06 部署时已核实：news 实际网络名为 news_news_net
```

> 若 news 网络名不同，用 `docker network ls` 查看后替换。若不想复用网络，可让 car-backend 通过宿主机 `127.0.0.1:3306` 访问（需确认 MySQL 端口已映射到宿主机）。

启动：

```bash
cd ~/car/backend
docker compose up -d
docker logs car-backend --tail 50   # 确认建表+种子数据成功
curl -s http://127.0.0.1:8091/api/health
```

### 3.4 nginx 配置（宿主机）

新建 `/etc/nginx/conf.d/car.conf`：

```nginx
server {
    listen 8090;
    server_name _;

    root /var/www/car/current;
    index index.html;

    location /api/ {
        proxy_pass http://127.0.0.1:8091;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
    location /uploads/ {
        proxy_pass http://127.0.0.1:8091;
    }
    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

发布前端（与 news 相同的 current 软链模式）：

```bash
sudo mkdir -p /var/www/car/releases
# 上传 dist 到 /var/www/car/releases/v1 后：
sudo ln -sfn /var/www/car/releases/v1 /var/www/car/current
sudo nginx -t && sudo systemctl reload nginx
```

### 3.5 验证

```bash
curl -s http://127.0.0.1:8090/ | grep -o "assets/[^"]*" | head -3   # 确认新版本资源
curl -s http://127.0.0.1:8090/api/health
```

手机浏览器访问 `http://<服务器IP>:8090`，注册账号开始使用。

---

## 4. 数据库维护

- **备份**：`docker exec news-mysql mysqldump -uroot -p123456 car_system > car_system_$(date +%F).sql`
- **迁移**：新增列用 `ALTER TABLE`（与 news 同习惯），执行后重启 car-backend。
- **数据导出**：系统内「车辆主页 → 导出数据」可导出该车全部 CSV 打包 zip。

---

## 5. 常用排障

| 现象 | 处理 |
|---|---|
| 前端打开是旧版本 | 检查 `/var/www/car/current` 软链是否指向新版本（勿只切 releases/current） |
| /api 404 或 502 | `docker logs car-backend --tail 50`；确认容器起在 8091、nginx 反代正确 |
| 登录后接口 401 | JWT_SECRET 变更导致旧 token 失效，重新登录即可 |
| AI 无解读 | 「设置」中给 DeepSeek 填 API Key 并启用；不配置则显示规则引擎结果 |
| 图片 404 | 检查 `/uploads/` 反代与 `~/car/backend/data/uploads` 目录 |
