# 停车模块（长租车位）方案 v1.0

## 一、模块概述

在汽车档案系统中增加"停车"模块，用于管理长租车位信息。与加油、保险模块保持一致的交互模式：列表 → 详情 → 编辑。

## 二、数据模型设计

表名：`parking_records`

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer, PK | 主键 |
| vehicle_id | Integer, FK | 所属车辆 |
| parking_address | String(200) | 车位地址，如"北京市朝阳区XX小区B1层" |
| parking_no | String(50) | 车位号码，如"B1-088" |
| charge_company | String(100) | 收费单位，如"XX物业管理有限公司" |
| start_date | Date | 起始时间（精确到天） |
| duration_months | Integer | 租期（按月为单位），如 12 |
| end_date | Date | 结束时间（**自动计算**：start_date + duration_months 个月） |
| amount | Integer | 金额（精确到元，单位：元） |
| note | String(500) | 备注（选填） |
| created_at | DateTime | 创建时间 |
| updated_at | DateTime | 更新时间 |

### 结束时间自动计算规则
- 后端：创建/更新时，根据 `start_date` 和 `duration_months` 自动计算 `end_date` 并入库
- 前端：编辑表单中选择起始时间和租期后，实时显示计算出的结束时间（只读展示）
- 月末溢出处理：如 1月31日 + 1个月 = 2月28日（或29日），与系统现有 `_add_months` 函数一致

## 三、后端 API 设计

路由前缀：`/api/parking`

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/parking?vehicle_id=X | 某车辆的停车记录列表（按起始时间倒序） |
| GET | /api/parking/:id | 单条停车记录详情 |
| POST | /api/parking | 新增（后端自动计算 end_date） |
| PUT | /api/parking/:id | 编辑（后端自动重新计算 end_date） |
| DELETE | /api/parking/:id | 删除 |

### 数据迁移
- `main.py` 中新增 `_ensure_parking_schema()` 幂等迁移函数，自动建表
- 与现有保险、加油模块迁移方式一致

## 四、前端页面与交互

### 1. 停车列表页 `ParkingListView.vue`
- 入口：车辆主页功能入口新增"停车"
- 列表每行展示：车位地址 + 车位号码（标题），租期范围 + 金额（副标题）
- 右上角"+"按钮新增
- **点击列表项 → 进入详情页**（与加油、保险一致）

### 2. 停车详情页 `ParkingDetailView.vue`
- 头部：车位地址 + 车位号码，金额大字展示
- 基本信息：收费单位、起始时间、租期（X个月）、结束时间
- 备注（有内容时展示）
- 底部操作：**编辑**（跳编辑页）、**删除**（确认后删除）

### 3. 停车编辑页 `ParkingFormView.vue`
- 新增和编辑共用
- 表单字段：
  - 车位地址（文本输入）
  - 车位号码（文本输入）
  - 收费单位（文本输入）
  - 起始时间（日期选择器，精确到天）
  - 租期（数字输入，单位：月，如 1/3/6/12）
  - 结束时间（**只读展示**，根据起始时间+租期实时计算）
  - 金额（数字输入，单位：元，精确到元）
  - 备注（文本域，选填）
- 底部保存按钮

## 五、路由设计

```
/vehicle/:id/parking           → 停车列表
/vehicle/:id/parking/:pid      → 停车详情
/vehicle/:id/parking/new       → 新增停车
/vehicle/:id/parking/:pid/edit → 编辑停车
```

## 六、车辆主页集成

- 功能入口列表新增"停车"项（图标 `location-o` 或 `shop-o`）
- 排在"保险"和"年检"之间，或"违章记录"之后

## 七、到期提醒（建议）

长租车位到期后需要续租或退租，建议增加到期提醒：

- **提醒时机**：到期前 30 天（1个月）开始提醒，与保险、年检保持一致
- **提醒内容**："车位到期：XX地址 XX车位号，将于 XXXX-XX-XX 到期，剩余 X 天，请及时续租或退租"
- **实现方式**：在 `remind_service.py` 中增加停车到期扫描，与保险、年检提醒逻辑一致
- 同一辆车多条停车记录时，每条单独提醒（因为可能是不同车位的连续租期）

> 此项为建议，如不需要可省略。

## 八、开发步骤

1. 后端：models.py 加 ParkingRecord 表 → schemas.py 加 ParkingIn/ParkingOut → routers/parking.py 新建 CRUD 路由 → main.py 加幂等迁移 → main.py 注册路由
2. （如做提醒）remind_service.py 加停车到期扫描
3. 前端：ParkingListView.vue → ParkingDetailView.vue → ParkingFormView.vue → router.js 加路由 → VehicleDetailView.vue 加功能入口
4. 本机验证 → 线上部署（后端 rebuild + 前端新版本）→ GitHub 推送

## 九、待确认事项

1. **到期提醒**是否需要？（建议需要，到期前1个月提醒）
2. **金额**是否需要区分"总金额"和"月均金额"？当前设计为总金额（租期内总共付了多少钱）
3. **车位地址和车位号码**是否合并为一个字段？当前分开，地址是位置描述，号码是具体车位编号
4. **备注字段**是否需要？（建议保留，选填）
