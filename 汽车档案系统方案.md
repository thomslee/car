# 汽车档案系统 · 系统方案（v0.2 已评审确认）

> 依据 `需求草稿.md` 整理。v0.1 已完成评审，本版记录评审决策并进入开发。
> 技术栈与既有 news-system 保持一致（Vue3 + FastAPI + MySQL），可复用部署经验。

## 0. 评审确认记录（2026-09-06）

| # | 评审意见 | 落地决策 |
|---|---|---|
| 1 | 系统可自行调入各品牌大模型，暂先调入 DeepSeek | 新增「模型管理」：可配置多品牌 LLM（base_url/api_key/model/能力），内置 DeepSeek 模板并设默认 |
| 2 | 需接入大模型，方案同一 | AI 以规则引擎为底座计算结构化结果，配置的默认大模型生成自然语言解读；未配置 key 时系统功能完整 |
| 3 | 需要有用户管理与登录功能 | 新增用户表 + 注册/登录 + JWT 鉴权，全部业务 API 需登录 |
| 4 | 上线使用腾讯云服务器，端口 8090 | 部署沿用 docker compose + nginx，服务端口 8090，需在云控制台放行 |
| 5 | 跟踪维度只加违章记录 | 新增违章记录模块（时间/地点/行为/扣分/罚款/处理状态/单据） |
| 6 | 车辆为沃尔沃，暂按该品牌 | 保养手册基准数据按沃尔沃官方基础保养周期内置（机油机滤 1 万公里/1 年等，来源见 3.8） |

---

## 1. 项目概述

### 1.1 目标

建立名下车辆的全生命周期档案系统，**以手机端为主要使用入口**，集中管理：

- 车辆档案（品牌、车架号、车牌等）
- 保养 / 维修记录（扫描录入 + 手工录入）
- 加油记录与油耗统计
- 保险、年检等证照与到期提醒
- 基于 AI 的保养预测、过度保养识别、费用估算

### 1.2 需求解读

| 需求草稿原文 | 落地功能 |
|---|---|
| 手机查看为主 | 移动端优先 H5（Vant 4），桌面浏览器可用，支持 PWA 添加到主屏 |
| 建立汽车档案 | 车辆档案：品牌/车系/年款/车架号/车牌/发动机号/购车日期/当前里程等 |
| 保养维修记录 | 保养/维修台账 + 项目明细（项目、材料费、工时费、里程、门店） |
| 扫描保养维修单录入 | 拍照 → 服务端 OCR 识别 → 自动填充表单草稿 → 人工确认保存；原图留存 |
| 手工录入 | 全部记录类型均支持手动新建 |
| 记录加油 | 加油台账 + 自动油耗统计（需记里程与是否加满） |
| 记录保险 | 保单台账（交强险/商业险、险种、保费、起止日期、附件） |
| 跟踪全生命周期 | 车辆主页汇总：里程、费用、保养/保险/年检状态、时间线 |
| AI 预测下次保养内容 | 规则引擎 + 保养手册基准，预测下次保养时间/里程与项目清单 |
| 提醒保险等事项 | 提醒中心：保险到期、年检到期、保养到期、自定义提醒 |
| AI 识别过度保养 | 实际保养间隔 vs 手册建议周期对比，标出过早项目并给出省钱建议 |
| 预测保养价格 | 参考价估算（用户历史均价优先 + 内置参考区间），明确标注为估算 |
| 前后端分离 + MySQL | Vue3 前端 + FastAPI 后端 + MySQL（库名 `car_system`） |

### 1.3 设计原则

1. **录入越省事越好**：拍照 OCR 一条龙，少打字；数据不重复录入。
2. **AI 可解释、可兜底**：AI 结论给出依据（哪条记录、哪个对比），规则引擎为底座，大模型只是可选的解读增强；无外部 API 时系统功能完整。
3. **宁缺毋滥**：沿用新闻系统的取舍原则——提醒与判断宁可保守，不制造噪音。
4. **单用户为主**：与 news-system 一致，不设计多用户体系；可选加一层简单登录保护敏感信息。
5. **数据可带走**：支持导出与备份，防止多年档案因换设备丢失。

---

## 2. 技术架构

### 2.1 技术栈

| 层 | 选型 | 说明 |
|---|---|---|
| 前端 | Vue 3（`<script setup>`）+ Vant 4 + Pinia + Vue Router + Vite | Vant 为移动端 UI 库，替代 news 用的 Element Plus，手机体验优先 |
| 图表 | ECharts（移动端适配） | 油耗趋势、费用报表 |
| 后端 | Python FastAPI + SQLAlchemy + PyMySQL + APScheduler | APScheduler 每日扫描到期提醒 |
| 文件处理 | python-multipart（图片上传）+ 本地存储 | 单据/保单图片存服务器本地目录 |
| 数据库 | MySQL（库名 `car_system`） | 本机与 news 同实例，服务器为独立库 |
| OCR（可选） | 第三方云 OCR（腾讯云/百度，待确认） | 拍照识别保养单字段 |
| 大模型（可选） | 兼容 OpenAI 协议 API（如豆包，待确认） | 生成自然语言健康报告，规则引擎照常可用 |

### 2.2 总体架构

（见评审回复中的架构图：终端层 → 前端应用层 → 后端服务层 → 数据层，外加 OCR / LLM 外部依赖）

```
手机浏览器 / PWA（主入口）        电脑浏览器（备用）
        │
   ┌────▼──────────────────────────────┐
   │ 前端：Vue3 + Vant4 H5              │
   │ 车辆档案 / 保养维修 / 加油油耗 /   │
   │ 保险年检 / 提醒中心 / AI分析 / 报表│
   └────┬──────────────────────────────┘
        │  REST API（/api/*，JSON）
   ┌────▼──────────────────────────────┐
   │ 后端：FastAPI                      │
   │ 路由层（vehicles/maintenance/…）  │
   │ 服务层：AI分析 · OCR · 提醒扫描 ·  │
   │         油耗统计 · 导出            │
   │ 定时任务：APScheduler 每日提醒检查  │
   └────┬──────────────────────────────┘
        │
   ┌────▼───────────┐   ┌───────────────┐
   │ MySQL car_system│   │ 本地图片存储   │
   └────────────────┘   └───────────────┘
        │                    │
   ┌────▼────────────────────▼──────────┐
   │ 外部依赖（可选）：云OCR API / LLM API│
   └────────────────────────────────────┘
```

### 2.3 部署形态

- **本机开发**：`E:\car`，本机 MySQL（已建 `car_system` 库，MySQL 8.4）。
- **上线部署**：复用 news-system 的腾讯云 docker compose + nginx 模式，新增独立容器，**服务端口 8090**（避开 80/8000/8001/8080 既有占用），云控制台放行 8090，手机通过域名/IP 访问。
- **启动脚本**：沿用 bat 脚本，注意保持 GBK 编码（news 系统踩过坑）。

---

## 3. 功能设计

### 3.1 功能模块总览

| 模块 | 核心能力 |
|---|---|
| 车辆档案 | 多车管理、车辆主页、里程打点、照片 |
| 保养维修 | 台账 + 项目明细 + 单据图片 + OCR 一键录入 |
| 加油油耗 | 加油台账、百公里油耗自动计算、油耗趋势 |
| 保险年检 | 保单台账、险种明细、年检记录、到期提醒 |
| 提醒中心 | 保养/保险/年检/自定义提醒，主页倒计时卡片 |
| AI 分析 | 保养预测、过度保养检测、价格估算、健康报告 |
| 模型管理 | 多品牌大模型可配置（DeepSeek 默认模板），各模块可选调用 |
| 违章记录 | 违章时间/地点/行为/扣分/罚款/处理状态/单据图片 |
| 用户与登录 | 注册/登录/JWT，业务数据按用户隔离 |
| 统计报表 | 年度/月度费用分类汇总、费用趋势 |
| 数据管理 | 导出 CSV/Excel、附件图片库、备份说明 |

### 3.2 车辆档案

- 字段：名称/别名（如"大白"）、品牌、车系、年款、车型、车架号 VIN、车牌、发动机号、购车日期、燃料类型、排量、变速箱、颜色、初始里程、当前里程、备注。
- 支持多辆车（家庭多车），可停用/归档（保留历史数据）。
- **车辆主页**（核心页面）：当前里程、油耗、最近保养、下次保养倒计时、待处理提醒（保险/年检/保养）、本年费用、最近记录时间线。
- 行驶证照片留存。

### 3.3 保养 / 维修记录

- 记录头：日期、里程、门店、类型（保养/维修/年检/其他）、标题、总费用、发票号、备注、单据图片（可多张）。
- 项目明细：项目名（机油/机滤/空滤/刹车片…）、数量、材料费、工时费、是否为定期保养项。
- 录入途径：
  1. **OCR 扫描**（见 3.4）；
  2. **手工录入**（含常用项目模板，点选即填，减少打字）；
  3. **从上次保养"一键续录"**：复制上次项目清单，只改价格/里程。
- 维修类记录额外支持：故障描述、是否在质保期内。

### 3.4 OCR 扫描录入（保养单/维修单/发票）

流程：

```
拍照/选图 → 上传 → 服务端 OCR → 字段抽取 → 表单草稿 → 人工确认/修正 → 保存
                                                        └── 原图自动存入附件库
```

- 抽取字段：日期、里程、门店名、项目名称、各项金额、总费用。
- 抽取方式：OCR 文本 → 规则解析（关键词匹配项目名、金额/里程正则）→ 置信度低的字段留空交给用户补。
- 失败兜底：识别不清时自动退化为"仅存原图 + 手工填写"，不影响录入。
- 原图永不删除，供日后核对。

### 3.5 加油与油耗

- 字段：日期、里程、加油量（升）、单价、金额、加油站、油品类型、是否加满、备注。
- **油耗自动计算**：相邻两次"加满"记录之间，用行驶里程 ÷ 加油量得出百公里油耗；首条加满记录为基准。
- 油耗趋势图（按加油次数/月份），可查看某段时间平均油耗。
- 里程打点：加油、保养都会更新车辆当前里程；也支持单独"里程打卡"（不加油也能记）。

### 3.6 保险与年检

- 保险：公司、保单号、类型（交强险/商业险）、险种明细（三者/车损/驾乘…及保额）、保费、起止日期、电子保单附件。
- 年检：检测日期、到期日期、检测站、结果、费用、附件。
- 到期前 N 天（默认 30 天，可在设置里调）自动进入提醒中心。

### 3.7 提醒中心

| 提醒类型 | 触发方式 |
|---|---|
| 保险到期 | 保单到期日提前 N 天 |
| 年检到期 | 年检到期日提前 N 天 |
| 保养到期 | 由 AI 保养预测产生（按时间或按里程，先到者优先） |
| 自定义 | 手动添加（如"轮胎 6 万公里更换"） |

- 每日定时扫描（APScheduler），生成/更新待办；主页展示最近 3 条，点击可处理（完成/忽略）。

### 3.8 AI 智能分析

| 能力 | 输入 | 输出 |
|---|---|---|
| 保养预测 | 历史保养记录 + 里程增速 + 车型手册基准 | 下次保养预计时间/里程、建议项目清单、预计费用区间 |
| 过度保养检测 | 各定期保养项的实际间隔 vs 手册建议 | 标记"过早/正常/过晚"，过早项给出建议间隔与可省费用说明 |
| 价格估算 | 项目名 + 车型级别 | 参考价区间（用户历史均价优先，其次内置参考表），标注"估算，以门店报价为准" |
| 健康评分（增强） | 保养及时性、里程强度、维修频率 | 0–100 分 + 简评（可选） |

- 实现方式：**规则引擎为底座**（可解释、可离线运行），大模型 API 仅用于生成自然语言解读（可选开关）。
- 所有结论附"依据"：引用的记录、对比的基准、计算过程摘要，用户可逐条核对。

### 3.9 统计报表

- 年度/月度费用汇总：保养、维修、加油、保险、年检分类统计与占比。
- 费用趋势图（折线/柱状）。
- 单车费用排行（多车时）。
- 所有数字由明细现算，不做手填汇总。

### 3.10 数据管理

- 导出：车辆全量数据导出 CSV / Excel（按表导出或一键打包）。
- 备份：提供 mysqldump 备份脚本说明；图片目录同步备份。
- 附件图片库：按车辆浏览全部留存的单据/保单/照片。

---

## 4. 数据模型设计（MySQL 库 `car_system`）

| 表 | 用途 | 关键字段 |
|---|---|---|
| `vehicles` | 车辆档案 | id, name, brand, series, model_year, vin, plate_no, engine_no, purchase_date, initial_mileage, current_mileage, fuel_type, displacement, transmission, color, is_active, notes, created_at, updated_at |
| `mileage_records` | 里程打点 | id, vehicle_id, recorded_at, mileage, source(手工/加油/保养), note |
| `maintenance_records` | 保养/维修记录头 | id, vehicle_id, occurred_at, mileage, shop_name, record_type(保养/维修/年检/其他), category, title, description, total_cost, invoice_no, warranty(是否质保), notes, created_at |
| `maintenance_items` | 记录项目明细 | id, record_id, item_name, quantity, part_cost, labor_cost, is_routine, note |
| `refuel_records` | 加油记录 | id, vehicle_id, refueled_at, mileage, fuel_amount_l, unit_price, total_cost, station, fuel_type, is_full, note |
| `insurance_policies` | 保单 | id, vehicle_id, company, policy_no, policy_type, items_json(险种明细), premium, start_date, end_date, attachment, note |
| `inspections` | 年检记录 | id, vehicle_id, inspected_at, expire_at, result, station, cost, attachment, note |
| `violation_records` | 违章记录 | id, vehicle_id, occurred_at, location, behavior, points, fine, status(未处理/已处理), handle_date, attachment, note |
| `users` | 用户 | id, username(唯一), password_hash, display_name, created_at |
| `ai_providers` | 大模型配置 | id, name, base_url, api_key, model_name, capabilities(text/vision), is_default, is_enabled |
| `reminders` | 提醒 | id, vehicle_id, remind_type(保养/保险/年检/自定义), target_date, target_mileage, threshold_days, message, status(待处理/已完成/已忽略), source_id, created_at |
| `attachments` | 附件图片 | id, vehicle_id, biz_type(保养单/保单/发票/行驶证/照片), biz_id, file_path, created_at |
| `ocr_tasks` | OCR 过程留痕 | id, vehicle_id, image_path, raw_text, parsed_json, status, created_at |
| `maintenance_manual` | 保养手册基准（内置种子数据） | id, brand, series, item_name, interval_km, interval_months, is_recommended |
| `reference_prices` | 参考价区间（内置+用户均值） | id, item_name, vehicle_level, price_min, price_max, unit, source |
| `settings` | 系统设置 | key, value（如默认提醒提前天数） |
| `ai_analyses` | AI 分析结果缓存 | id, vehicle_id, analysis_type, result_json, created_at |

- 关系：`vehicles` 1—N 各记录表；`maintenance_records` 1—N `maintenance_items`；`attachments` 通过 `biz_type + biz_id` 挂到任意记录。
- 数据库迁移沿用 news 做法：新增列用 `ALTER TABLE`，提供迁移脚本。

---

## 5. 后端 API 设计（概览）

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/auth/register` `/api/auth/login` `/api/auth/me` | 注册 / 登录 / 当前用户 |
| GET/POST | `/api/vehicles` | 车辆列表 / 新增 |
| GET/PUT/DELETE | `/api/vehicles/{id}` | 详情 / 更新 / 归档删除 |
| GET | `/api/vehicles/{id}/summary` | 车辆主页汇总（里程/提醒/下次保养/费用） |
| GET/POST | `/api/vehicles/{id}/mileage` | 里程打点 |
| GET/POST/PUT/DELETE | `/api/maintenance` 、`/api/maintenance/{id}` | 保养维修 CRUD（含明细） |
| POST | `/api/maintenance/{id}/items` | 项目明细增删 |
| GET/POST | `/api/refuels` 、`/api/refuels/{id}` | 加油 CRUD |
| GET | `/api/vehicles/{id}/fuel-stats` | 油耗统计与趋势数据 |
| GET/POST | `/api/insurance` 、`/api/insurance/{id}` | 保单 CRUD |
| GET/POST | `/api/inspections` | 年检 CRUD |
| GET/POST | `/api/violations` 、`/api/violations/{id}` | 违章 CRUD |
| GET/POST | `/api/ai/providers` 、`PUT/DELETE /api/ai/providers/{id}` | 大模型配置管理 |
| GET | `/api/reminders` | 提醒列表（按车辆/状态筛选） |
| POST | `/api/reminders/{id}/handle` | 处理提醒（完成/忽略） |
| POST | `/api/ocr/upload` | 上传单据图片 → 返回识别草稿 |
| POST | `/api/ai/maintenance-plan` | 保养预测 |
| POST | `/api/ai/over-maintenance` | 过度保养检测 |
| POST | `/api/ai/price-estimate` | 价格估算 |
| POST | `/api/ai/health-report` | 健康报告（可选 LLM） |
| GET | `/api/stats/annual-cost` | 年度费用统计 |
| GET | `/api/export` | 数据导出 |
| GET | `/api/health` | 健康检查 |

---

## 6. 实用功能增强清单（相对需求草稿新增）

1. **油耗自动统计与趋势图**：草稿只要求"记录加油"，本方案让加油数据自动变成油耗洞察。
2. **车辆主页仪表盘**：一屏看到里程、倒计时、待办、费用，符合"手机查看"诉求。
3. **里程打点**：不加油也能记里程，支撑更准的保养预测。
4. **年度费用报表**：把散落的记录变成"养这辆车一年花多少"。
5. **保养手册基准数据**：内置常见车型/项目的建议周期，是"过度保养检测"的对比基础。
6. **参考价区间表**：支撑保养价格预测，且优先用你自己的历史消费均值，越用越准。
7. **多车辆支持**：家庭多车、换车归档。
8. **附件图片库**：所有单据照片集中留存，随时可查。
9. **数据导出与备份**：档案数据可带走。
10. **PWA 手机桌面化**：添加到主屏后像 App 一样使用（可作上线阶段增强）。

---

## 7. 安全与隐私

- 车架号、车牌、保单号属于个人敏感信息：数据库密码不硬编码（`.env`），可选加**简单账号登录**（单账号密码）。
- 单据图片默认存本机/自有服务器；**调用云 OCR 时需用户知情同意**（图片会发送至第三方）。
- 大模型 API 调用只传结构化摘要，不传完整图片；敏感字段（VIN/车牌）默认脱敏后再发送。
- 提供备份脚本，防止多年档案丢失。

---

## 8. 开发计划（确定后执行）

| 阶段 | 范围 | 验收标准 |
|---|---|---|
| **一：核心可用** | 车辆档案、保养维修手工录入、加油与油耗、保险、年检、里程、提醒中心、车辆主页 | 手机端可完整记录日常养车数据，到期提醒正常 |
| **二：智能便捷** | OCR 扫描录入、AI 保养预测、过度保养检测、价格估算 | 拍单可录入；AI 结论可解释、有依据 |
| **三：完善上线** | 年度报表、导出备份、PWA、可选 LLM 健康报告、部署到服务器 | 手机可访问正式版，数据可备份可导出 |

- 每阶段结束交付可运行版本供试用，再进入下一阶段。
- 数据模型在阶段一定稿，阶段二/三只增表不改旧表。

---

## 9. 评审结论

已按第 0 节评审记录全部确认。OCR 采用渐进方案：图片始终本地留存；若配置的模型支持视觉（如豆包/Qwen-VL）则自动走图片识别；DeepSeek 为文本模型，配合「粘贴单据文本 → DeepSeek 结构化抽取」通道 + 手工确认，不依赖第三方 OCR 云服务。

## 10. 沃尔沃保养基准数据来源

种子数据基于沃尔沃官方基础保养周期（volvocars.com.cn S60 保养页，2026-09 检索）与车主手册共识整理：

| 项目 | 建议周期 | 说明 |
|---|---|---|
| 机油及机油滤清器 | 1 万公里 / 12 个月 | 以先到为准 |
| 空调滤芯 | 2 万公里 / 24 个月 | 空气质量差可提前 |
| 发动机空滤 | 3 万公里 / 36 个月 | 官方第 3 年项目 |
| 燃油滤清器 | 3 万公里 / 36 个月 | 官方第 3 年项目 |
| 火花塞 | 4 万公里 / 48 个月 | 官方第 4 年项目 |
| 制动液 | 4 万公里 / 24 个月 | 官方手册常见 2 年 |
| 补胎液检查 | 2 万公里 / 24 个月 | 官方第 2 年项目 |

来源：[沃尔沃官方 S60 保养页](https://www.volvocars.com.cn/zh-cn/l/gpss/s60/)。基准为辅助判断，实际以车辆手册与门店建议为准。
